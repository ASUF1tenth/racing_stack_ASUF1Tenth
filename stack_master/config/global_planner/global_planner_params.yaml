global_planner:
  ros__parameters:
    rate: 10  # [hz]
    required_laps: 0  # how many laps to drive until calculating global waypoints
    safety_width: 0.2  # [m] including the width of the car
    safety_width_sp: 0.2  # [m] including the width of the car for shortest path optimization
    occupancy_grid_threshold: 30
    filter_kernel_size: 3 # how much filtering to do to raw map images
    reverse_mapping: False  # By default, do not reverse the direction of the track

    show_plots: False
