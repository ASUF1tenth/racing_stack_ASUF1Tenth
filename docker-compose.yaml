version: '3.3'
services:
  nuc:
    platform: linux/arm64
    build:
      context: .
      dockerfile: .devcontainer/Dockerfile
      args:
        - USERNAME=${USER}
        - UID=${UID}
        - GID=${GID}
    image: nuc_forzaeth_racestack_ros2:jazzy
